#include <stdio.h>
#include "imagesRW.h"
#include "fft-a-prog.h"

int main(int argc,char **argv)
{
  
  /* Voici les différents tests à effectuer
  sinusoidale_1d(64,3);
  ou
  TEST1D();
  ou
  TEST2D();
  ou
  mire_sinus (64,3);
  ou
  FFT_IM();
  */
  
    /*
    sinusoidale_1d(64,3);
    TEST1D();
    TEST2D();
    mire_sinus (64,3);
    FFT_IM();
    */
    FFT_IM();

  return (0);
}
